# App Configuration (required)
APP_NAME=MyApp
XCODEPROJ=MyApp.xcodeproj
SCHEME=MyApp
TARGET=MyApp
GITHUB_REPOSITORY=username/repo
DEVELOPER_ID_APPLICATION=Developer ID Application: Your Name (TEAMID)

# Optional
MACOS_MIN_VERSION=14.0
ASC_API_KEY_PATH=api_key.json

# GitHub (required for releases)
# Create at: https://github.com/settings/tokens
# Required scopes: repo (for private repos) or public_repo (for public repos)
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Match (required for signed builds)
# Certificates are stored encrypted in a git repo
MATCH_GIT_URL=git@github.com:username/certs.git
MATCH_PASSWORD=your-encryption-password

# App Store Connect API Key (required for notarization)
# Place api_key.json in fastlane/ directory
# Create at: https://appstoreconnect.apple.com/access/integrations/api

# Sparkle EdDSA private key (required for generate_appcast)
# Generate with: ./Sparkle.framework/bin/generate_keys
# Store the private key here, add the public key to Local.xcconfig
SPARKLE_PRIVATE_KEY=your-base64-ed25519-private-key
